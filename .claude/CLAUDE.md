# claude-vibeline

- The OAuth usage API (`/api/oauth/usage`) is undocumented and unstable
- The cache timer refresh relies on toggling a trailing space in `settings.json` to trigger Claude Code's config watcher — this re-render mechanism is undocumented and unstable
